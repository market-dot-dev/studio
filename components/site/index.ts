import TestComponent from "./test-component";
const componentsMap = {
    testcomponent: {
        name: 'Test Component',
        tag: 'TestComponent',
        element: TestComponent
    }
} as any;

export default componentsMap;