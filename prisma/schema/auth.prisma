model User {
  id                          String         @id @default(cuid())
  name                        String?
  username                    String?
  gh_id                       Int? // @TODO: Migrate this somehow.
  gh_username                 String?        @unique // @TODO: Migrate this somehow.
  email                       String?        @unique
  businessLocation            String? // @TODO: Migrate this somehow.
  businessType                String? // @TODO: Migrate this somehow.
  stripeCustomerIds           Json           @default("{}") // @TODO: Migrate this somehow.
  stripePaymentMethodIds      Json           @default("{}") // @TODO: Migrate this somehow.
  stripeAccountId             String?        @unique // @TODO: Migrate this somehow.
  stripeCSRF                  String?        @unique // @TODO: Migrate this somehow.
  stripeAccountDisabled       Boolean        @default(false) // @TODO: Migrate this somehow.
  stripeAccountDisabledReason String? // @TODO: Migrate this somehow.
  projectName                 String? // @TODO: Migrate this somehow.
  projectDescription          String? // @TODO: Migrate this somehow.
  emailVerified               DateTime?
  image                       String?
  roleId                      String         @default("customer") // @TODO: Migrate this somehow.
  createdAt                   DateTime       @default(now())
  updatedAt                   DateTime       @updatedAt
  accounts                    Account[]
  pages                       Page[] /// @DEPRECATED. Should be fetched from Organization.
  sessions                    Session[]
  sites                       Site[] /// @DEPRECATED. Should be fetched from Organization.
  tiers                       Tier[] /// @DEPRECATED. Should be fetched from Organization.
  subscriptions               Subscription[] /// @DEPRECATED. Should be fetched from Organization.
  leads                       Lead[] /// @DEPRECATED. Should be fetched from Organization.
  onboarding                  String?
  company                     String? // @TODO: Migrate this somehow.
  charges                     Charge[] /// @DEPRECATED. Should be fetched from Organization.
  contracts                   Contract[] /// @DEPRECATED. Should be fetched from Organization.
  prospects                   Prospect[] /// @DEPRECATED. Should be fetched from Organization.
  marketExpertId              String? // @TODO: Migrate this somehow.

  // Organization relationships
  organizations         OrganizationMember[]
  ownedOrganizations    Organization[]       @relation("OwnedOrganizations")
  currentOrganizationId String?
  currentOrganization   Organization?        @relation("CurrentOrganization", fields: [currentOrganizationId], references: [id], onDelete: SetNull)
}

model Account {
  id                       String  @id @default(cuid())
  userId                   String
  type                     String
  provider                 String
  providerAccountId        String
  refresh_token            String?
  refresh_token_expires_in Int?
  access_token             String?
  expires_at               Int?
  token_type               String?
  scope                    String?
  id_token                 String?
  session_state            String?
  oauth_token_secret       String?
  oauth_token              String?
  user                     User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
